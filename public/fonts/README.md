# 手記字體子集

`HuiwenMincho-notes-subset.woff2` 是從 `@phenomcanvas/ui` v0.1.6 所附的
`HuiwenMincho-subset.woff2` 再裁出的手記站專用子集。它只保留目前介面、文章與短記會用到的字，
用來避免首頁為一套中文字體先下載 8.1 MB。原字體的來源與授權說明見 UI 套件的
`fonts/SOURCE-NOTES.md` 與 `fonts/LICENSES.md`。
