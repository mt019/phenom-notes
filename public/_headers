# 檔名帶內容雜湊的產物，內容永遠不會變，所以給一年加 immutable。
# Cloudflare Pages 的預設是 public, max-age=14400, must-revalidate，等於每四小時要回頭
# 問一次；而這些檔案換內容就會換檔名，問了也只會拿到同一份。
# 出處：MDN 快取指南「Cache Busting」與「Avoiding revalidation」，讀書筆記在
# phenom-ops/docs/ops-learning/notes/http-caching.md 第 2 條。
#
# 路徑帶 /notes 是因為 vite 的 base 是 /notes/。這個檔本身必須在部署目錄的根，而本站的
# scripts/stage-pages.mjs 會把整個 dist 搬進 dist/notes，所以那支腳本另外把它搬回根層；
# 少了那一步，這個檔會躺在 dist/notes/_headers，不會報錯，只是安靜地沒有作用。
# phenom-ops 的 smoke-pages.mjs 有一條檢查在擋這件事。
/notes/assets/*
  Cache-Control: public, max-age=31536000, immutable

# deployment-manifest.json 是拿來答「線上這份是不是這次部署的」的，任何一層快取存了它，
# 部署驗證就會讀到舊的答案。這裡是它的來源站，所以在這裡下 no-store 才是治本；canvas 那邊
# 的代理照樣會把這個標頭帶下去。stage-pages 會把它同時放在根層與 /notes 底下，兩條都要寫。
/deployment-manifest.json
  Cache-Control: no-store, max-age=0
/notes/deployment-manifest.json
  Cache-Control: no-store, max-age=0
